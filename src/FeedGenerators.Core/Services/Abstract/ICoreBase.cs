using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace FeedGenerators.Core.Services.Abstract
{
    // This is the interface that's used for DI. If a service inside FeedGenerators.Core implements this interface, then it can be injected from outside
    public interface ICoreBase
    {
    }
}
